import Chatbot from "../components/Chatbot";

function ChatbotPage() {
    return (
        <div className="chatbot-page" style={{ padding: "2rem", minHeight: "calc(100vh - 200px)" }}>
            <Chatbot />
        </div>
    );
}

export default ChatbotPage;
